import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    background: {
      default: "#f0f2f5",
    },

    text: {
      primary: "#333333",
      focus: "#333333",
    },

    primary: {
      main: "#e91e63",
      focus: "#e91e63",
    },
  },
});
